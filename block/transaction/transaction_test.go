package transaction

import (
	"testing"
)

// just to silence [transaction tests have been removed after some time as it would need to create a valid transaction which means it would need to know the password for the private key file and that node would also have to currently own tokens for a valid transaction to be created.]
func TestMain(t *testing.T) { }